<!DOCTYPE html>
<html>
  <head>
    <title>Taco Recipes</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />
  </head>

  <body>
    <h1>Taco Town 🌮</h1>

    <form action="/recipe" method="POST" class="buttons">
      <button type="submit" value="0001" name="choice">🍗</button>
      <button type="submit" value="0002" name="choice">🥩</button>
      <button type="submit" value="0003" name="choice">🐟</button>
    </form>
    <div id="recipeContainer" class="hidden">
      <!-- If no recipe yet, show: -->
      <% if (locals.item) { %>
      <h2><%= item.name %></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <%= item.ingredients.protein.name %>, <%=
          item.ingredients.protein.preparation %>
        </li>
        <li><%= item.ingredients.salsa.name %></li>
        <% for (let topping of item.ingredients.toppings) { %>
        <li><%= topping.quantity %> of <%= topping.name %></li>
        <% } %>
      </ul>
      <% } else { %>
      <h2>Pick your favourite taco ingredient 👆</h2>
      <% } %>
      <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
    </div>
  </body>
</html>
